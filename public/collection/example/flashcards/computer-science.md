# Computer Science

Q: What is the time complexity of binary search?
A: $O(\log n)$ - halves the search space each iteration.

C: A [stack] uses Last-In-First-Out (LIFO), while a [queue] uses First-In-First-Out (FIFO).
