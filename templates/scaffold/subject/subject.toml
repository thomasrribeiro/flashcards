schema_version = 3
subject = "{{SUBJECT}}"
destination = "{{DESTINATION}}"
focus = [{{FOCUS}}]

# The subject agent replaces this empty scaffold with a topologically ordered
# curriculum. Local prerequisite references name earlier deck ids in this
# subject; cross-subject references use "subject/deck" and must exist in the
# collection-wide catalog. Determine deck boundaries from coherent learning
# capabilities without deck-count quotas or chapter estimates.
#
# [[decks]]
# id = "foundations"
# order = 1
# tier = "core"
# level = "foundational"
# prerequisites = []
# recommended_after = []
# status = "proposed"
# description = "Durable capabilities established by this deck."
#
# [[coverage]]
# domain = "foundations"
# disposition = "included"
# decks = ["foundations"]
# rationale = "Required foundation for the requested destination."
